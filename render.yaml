services:
  - type: web
    name: my-app
    env: python
    runtime: python-3.12.0
    buildCommand: "pip install -r requirements.txt && npm install && npm run build"
    startCommand: "python server.py"  # or whatever your server file is